import {
  StyleSheet,
} from 'react-native';

const Styles = StyleSheet.create({
  listView: {
    alignItems: 'center',
    justifyContent: 'center',
    width: '100%',
  },

  title: {
    alignSelf: 'flex-start',
    fontSize: 20,
  },

  flatList: {
    padding: 10,
  },
});

export default Styles;
